An LLM is a type of AI model that excels at understanding and generating human language.
They are trained on vast amounts of text data, allowing them to learn patterns, structure, 
and even nuance in language. These models typically consist of many millions of parameters.  

Transformer architecture—a deep learning architecture based on the “Attention” algorithm.  
There are 3 types of transformers:  
1.Encoders   
 BERT from Google  
 Use Cases: Text classification, semantic search, Named Entity Recognition  
2. Decoders
  Example: Llama from Meta  
  Use Cases: Text generation, chatbots, code generation  
3. Seq2Seq (Encoder–Decoder)   
  Example: T5, BART
  Use Cases: Translation, Summarization, Paraphrasing

  
Next token prediction  
LLMs are said to be autoregressive, meaning that the output from one pass becomes the input for the next one. The loop ends at EOS token.

![AutoRegression](Image/AutoregressionSchema.gif)
*From HuggingFace Agents course*

![DecodingFinal](Image/DecodingFinal.gif)
*From HuggingFace Agents course*


## Attention
When predicting the next word, not every word in a sentence is equally important.
![Attention](Image/AttentionSceneFinal.gif)
*From HuggingFace Agents course*  


### Prompting the LLM is important  
**Considering that the only job of an LLM is to predict the next token by looking at every input token, and to choose which tokens are “important”, the wording of your input sequence is very important.**  
**The input sequence you provide an LLM is called a prompt. Careful design of the prompt makes it easier to guide the generation of the LLM toward the desired output.**  





